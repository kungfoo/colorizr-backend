require "ext/colorizr_image_sorter_native"

class ColorizrImageSorter
  # sorts both the arrays using parallel quicksort.
  # the scores array needs to contain Float values!!
  def sort!(scores, values)
    # implementation: see ext/colorizr_image_sorter_native
  end
end
